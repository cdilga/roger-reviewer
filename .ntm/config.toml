# Project-specific NTM configuration
# Overrides global settings for this project

[project]
name = "roger-reviewer"
created = "2026-03-30T01:46:01Z"

[integrations]
agent_mail = true
beads = true
cass = true
cm = true

agent_mail_project_key = "/Users/cdilga/Documents/dev/roger-reviewer"
agent_mail_registered = true
agent_mail_registered_at = "2026-03-30T01:46:01Z"
[defaults]
# Project-level spawn defaults. Use these instead of [agents], because
# project configs intentionally do not override agent command templates.
# agents = { cc = 2, cod = 1 }

[palette]
# file = "palette.md"  # Relative to .ntm/

[palette_state]
# pinned = ["build", "test"]
# favorites = ["build"]

[templates]
# dir = "templates"    # Relative to .ntm/
